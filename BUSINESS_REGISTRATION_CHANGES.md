# ثبت‌نام مستقل نمایشگاه و نمایندگی

در این نسخه، «نمایشگاه» و «نمایندگی» دو نوع مستقل کسب‌وکار هستند:

- `gallery`: نمایشگاه یا بنگاه خریدوفروش خودرو
- `agency`: شرکت حقوقی واردکننده خودرو

انتخاب نمایندگی از ابتدا کاربر و پروفایل کسب‌وکار را با نوع `agency` می‌سازد. اشتراک فقط امکانات تجاری را فعال می‌کند و هیچ‌گاه نقش کاربر یا نوع کسب‌وکار را تغییر نمی‌دهد.

## اطلاعات مشترک

- نام کسب‌وکار یا نام حقوقی شرکت
- تلفن ثابت، استان، شهر، آدرس کامل و کد پستی
- معرفی کسب‌وکار

## اطلاعات اختصاصی نمایشگاه

- شماره پروانه کسب
- مرجع صادرکننده پروانه
- تاریخ اعتبار پروانه
- شناسه ملی، در صورت حقوقی‌بودن نمایشگاه

## اطلاعات اختصاصی نمایندگی/شرکت واردکننده

- شناسه ملی و شماره ثبت شرکت
- کد اقتصادی و نماینده قانونی
- شماره، مرجع صادرکننده و تاریخ اعتبار مجوز واردات
- شماره کارت بازرگانی
- برندهای وارداتی

پروفایل هر دو نوع تا زمان تأیید مدیریت عمومی نمی‌شود و کسب‌وکار تأییدنشده اجازه ثبت آگهی ندارد. اصلاح اطلاعات هویتی، پروفایل تأییدشده را دوباره وارد صف بررسی همان نوع کسب‌وکار می‌کند.

## پنل مدیریت

- تب نمایشگاه‌ها فقط پروفایل‌های `gallery` را نمایش می‌دهد.
- تب نمایندگی‌ها فقط پروفایل‌های `agency` را نمایش می‌دهد؛ حتی اگر هنوز در انتظار بررسی باشند.
- درخواست اشتراک برای هر نوع کسب‌وکار جداست و تأیید آن هویت حساب را تغییر نمی‌دهد.

## مهاجرت پایگاه داده

پس از استقرار بک‌اند اجرا شود:

```bash
python manage.py migrate
```

مهاجرت `businesses.0002_separate_gallery_and_agency` درخواست‌های باز قدیمی را که با الگوی «نمایشگاه + اشتراک agency» ذخیره شده‌اند، به نوع `agency` منتقل می‌کند. این پرونده‌ها برای تکمیل اطلاعات شرکت واردکننده دوباره در وضعیت `pending` قرار می‌گیرند و در تب نمایندگی‌ها نمایش داده می‌شوند.
